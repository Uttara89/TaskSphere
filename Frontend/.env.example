# Frontend environment variables
# Copy to .env and adjust per environment

# Base URL for your API server (no trailing slash)
VITE_API_BASE_URL=http://localhost:3000

# Optional: separate socket URL (defaults to VITE_API_BASE_URL if unset)
# VITE_SOCKET_BASE_URL=http://localhost:3000
